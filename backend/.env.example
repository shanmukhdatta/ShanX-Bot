# ShanXBot Backend Environment Variables
# Built by Shanmukh Datta

# Optional: Set API keys here as fallback if not provided via headers
GROQ_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# Server config
HOST=0.0.0.0
PORT=8000

# ─── Firebase (optional — for server-side verification) ──────────────────────
# Not required unless you want the backend to verify Firebase ID tokens.
# FIREBASE_PROJECT_ID=your-project-id
